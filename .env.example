# Copia este archivo como .env y completa las variables

# ============================================================================
# CONFIGURACIÓN BÁSICA
# ============================================================================

# API Key de Groq para el chat con IA
# Obtén tu key en: https://console.groq.com/
GROQ_API_KEY=your_groq_api_key_here

# Puerto del servidor (opcional, Railway lo asigna automáticamente)
# PORT=3000

# Entorno de ejecución
NODE_ENV=production

# Dominio permitido para CORS
ALLOWED_ORIGIN=https://neotesisperu.online

# ============================================================================
# BASE DE DATOS (PostgreSQL)
# ============================================================================

# Para Local (Docker): postgres://neotesis:local_password@localhost:5432/neotesis_db
# Para Railway: Se genera automáticamente al crear el servicio PostgreSQL
# La variable DATABASE_URL se configura en Railway Dashboard > Variables
DATABASE_URL=postgres://user:password@hostname.railway.app:5432/neotesis

# ============================================================================
# CONFIGURACIÓN DE SEGURIDAD
# ============================================================================

# Secret para JWT (IMPORTANTE: usar un string largo y aleatorio, mínimo 32 caracteres)
# Genera uno seguro con: openssl rand -base64 32
JWT_SECRET=neotesis_peru_super_secret_key_minimo_32_caracteres_aqui

# Webhooks para alertas de seguridad (opcional)
# Configurar en servicios como Discord, Slack o sistemas de monitoreo

# Discord webhook para alertas críticas
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN

# Slack webhook para alertas generales
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK

# Webhook genérico para sistemas de monitoreo
MONITORING_WEBHOOK_URL=https://your-monitoring-service.com/webhook
MONITORING_API_KEY=your_monitoring_api_key

# ============================================================================
# CONFIGURACIÓN DE RATE LIMITING
# ============================================================================

# Deshabilitar cuotas para testing (true/false)
DISABLE_QUOTA=false

# Nivel de logging (error, warn, info, security, debug)
LOG_LEVEL=info
